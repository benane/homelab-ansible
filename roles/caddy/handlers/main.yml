---
- name: caddy neu starten
  ansible.builtin.systemd:
    name: caddy
    state: restarted
    daemon_reload: true

- name: caddy neu laden
  ansible.builtin.systemd:
    name: caddy
    state: reloaded
