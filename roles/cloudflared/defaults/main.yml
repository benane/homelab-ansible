cloudflared_config_dir: /etc/cloudflared
cloudflared_tunnel_id: b43d6744-9d65-4f85-bf37-bd237643a0d0
cloudflared_credentials_file: "{{ cloudflared_config_dir }}/{{ cloudflared_tunnel_id }}.json"
cloudflared_ingress: []
cloudflared_default_origin: "https://{{ hostvars['lxc-caddy'].ansible_host }}"

cloudflared_ingress_effective: "{{ cloudflared_ingress + service_registry | to_cloudflared_ingress(external_domain, cloudflared_default_origin) }}"

