---
tunnel: "{{ cloudflared_tunnel_id }}"
credentials-file: "{{ cloudflared_credentials_file }}"
originRequest:
  matchSNItoHost: true
ingress:
{{ cloudflared_ingress | to_nice_yaml(indent=2) | indent(2, first=true) }}
metrics: "{{ ansible_host }}:{{ cloudflared_metrics_port }}"
