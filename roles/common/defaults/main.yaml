---
# Standard-System-Pakete für jedes Debian-basierte System
common_base_packages:
  - sudo
  - curl
  - wget
  - git
  - htop
  - python3
  - prometheus-node-exporter

# Standard-Shell für neue User
common_default_shell: /bin/bash
